name: ark-check
description: Run arkctl check against a project (JSON or SARIF for CI)
author: manuelebeh

inputs:
  path:
    description: Project path to check
    required: false
    default: "."
  format:
    description: Output format (json | sarif | text)
    required: false
    default: "json"
  catalog:
    description: Optional user catalog directory (--catalog)
    required: false
    default: ""
  version:
    description: npm package version of arkctl to install
    required: false
    default: "0.5.0"

runs:
  using: composite
  steps:
    - name: Setup Node.js
      uses: actions/setup-node@v4
      with:
        node-version: "20"

    - name: Install arkctl
      shell: bash
      run: npm install -g "arkctl@${{ inputs.version }}"

    - name: Run arkctl check
      shell: bash
      run: |
        set -euo pipefail
        args=(check "${{ inputs.path }}" --format "${{ inputs.format }}")
        if [ -n "${{ inputs.catalog }}" ]; then
          args+=(--catalog "${{ inputs.catalog }}")
        fi
        arkctl "${args[@]}"
