schema_version: 1

id: php-feature
name: PHP feature folders
version: 0.1.0
description: >
  Vanilla PHP vertical features under src/Features/{Feature}/ with a public entrypoint.

requires:
  project_manifest: ">=1"

applies_to:
  stacks: [php]
  languages: [php]

files:
  layout: LAYOUT.md
  tree: tree.schema.yaml
  conventions: conventions.yaml
  agent_hints: agents.hints.yaml

checks:
  - tree
  - conventions
  - forbidden_paths
  - required_files

default_severity: error
severity:
  missing-root: error
  forbidden-path: error
  module-naming: warn
  missing-module-file: error
  denied-import: error
  cross-module-import: error
