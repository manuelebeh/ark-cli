schema_version: 1

id: rust-crate
name: Rust crate
version: 0.1.0
description: >
  Standard Rust binary crate layout: src/ with optional tests/, benches/, examples/, docs/.

requires:
  project_manifest: ">=1"

applies_to:
  stacks: [rust]
  languages: [rust]

files:
  layout: LAYOUT.md
  tree: tree.schema.yaml
  conventions: conventions.yaml
  agent_hints: agents.hints.yaml

checks:
  - tree
  - conventions
  - forbidden_paths
  - required_files

default_severity: error
severity:
  missing-root: error
  forbidden-path: error
  module-naming: warn
  missing-module-file: error
  denied-import: error
  cross-module-import: error
