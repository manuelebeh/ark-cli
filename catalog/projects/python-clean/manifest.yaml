schema_version: 1
id: python-clean
name: Python app (clean)
version: 0.1.0
description: Minimal Python app (clean) scaffold

implements:
  architecture: python-clean
  architecture_version: ">=0.1.0 <1.0.0"

stack:
  language: python
  runtime: python
  tags: [python]
  file_map:
    index: application/
    test_suffix: test_.py

source:
  root: ./template
