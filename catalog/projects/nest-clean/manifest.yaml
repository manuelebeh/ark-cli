schema_version: 1
id: nest-clean
name: NestJS app (clean)
version: 0.1.0
description: Minimal NestJS clean-architecture scaffold

implements:
  architecture: nest-clean
  architecture_version: ">=0.1.0 <1.0.0"

stack:
  language: typescript
  runtime: node
  tags: [nest, typescript, api]
  file_map:
    index: index.ts
    test_suffix: .spec.ts

source:
  root: ./template
