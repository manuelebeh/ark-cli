schema_version: 1
id: python-feature
name: Python app (feature)
version: 0.1.0
description: Minimal Python app (feature) scaffold

implements:
  architecture: python-feature
  architecture_version: ">=0.1.0 <1.0.0"

stack:
  language: python
  runtime: python
  tags: [python]
  file_map:
    index: __init__.py
    test_suffix: test_.py

source:
  root: ./template
