schema_version: 1
id: nest-hexagonal
name: NestJS app (hexagonal)
version: 0.1.0
description: Minimal NestJS hexagonal scaffold

implements:
  architecture: nest-hexagonal
  architecture_version: ">=0.1.0 <1.0.0"

stack:
  language: typescript
  runtime: node
  tags: [nest, typescript, api]
  file_map:
    index: index.ts
    test_suffix: .spec.ts

source:
  root: ./template
